// Transfer learning
// feature extractor for layers of neural network
// mobile net can be turned into a feature extractor by removing the last layer of the neural network and assigning our own labels.
